import { TextRestrainPipe } from './text-restrain.pipe';

describe('TextRestrainPipe', () => {
  it('create an instance', () => {
    const pipe = new TextRestrainPipe();
    expect(pipe).toBeTruthy();
  });
});
